apiVersion: v1
kind: Service
metadata:
  name: dtx-dashboard-backend-preview-svc
spec:
  selector:
    app: dtx-dashboard-backend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80